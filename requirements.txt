# Combined dependencies for the merged Render deployment (server.py).
# backend/requirements.txt and chatbot-service/requirements.txt still exist
# separately for running each service standalone in local dev.

# ---- Backend - Flask (backend/) ----
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Cors==4.0.1
Werkzeug==3.0.3
SQLAlchemy==2.0.31
psycopg2-binary==2.9.9

# ---- Chatbot service - FastAPI (chatbot-service/) ----
fastapi
uvicorn
huggingface_hub
numpy
scikit-learn
pydantic
openai==1.51.0

# ---- Shared ----
python-dotenv

# ---- Merge adapter: mounts the Flask app inside FastAPI as one process ----
a2wsgi