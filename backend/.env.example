# Flask secret key used to sign the admin session cookie
SECRET_KEY=change-this-in-production

# SQLite by default; point this at Postgres/MySQL for production
DATABASE_URL=sqlite:///instance/ruchi.db

# Default admin account, created automatically on first run
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password

PORT=5000
